numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
# Gradio 5 works with current huggingface_hub (HfFolder removed in hub 1.x)
gradio>=5.5.0,<6
huggingface_hub>=0.26.0,<1.0
pillow>=10.0
# Python 3.13+ removed stdlib audioop; Gradio/pydub need this shim if 3.13 is used
audioop-lts>=0.2.1; python_version>='3.13'
